name: Validate activity-support web signal v08
on:
  pull_request:
  push:
    branches: [main]
    paths:
      - 'data/evals/scenario_lab_websignals_v08.json'
      - 'scripts/validate_websignal_v08.py'
      - '.github/workflows/websignal-v08.yml'
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/validate_websignal_v08.py
